services:
  db:
    image: pgvector/pgvector:pg16

    container_name: pgvector-db

    environment:
      POSTGRES_USER: Shreya
      POSTGRES_PASSWORD: Shreya_22
      POSTGRES_DB: taskdb

    ports:
      - "5432:5432"

    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: